#Если Сервер Тогда

Процедура ВыполнитьЗагрузкуИзФайла(ПутьКФайлу) Экспорт

	ДанныеJSON = ИнтеграцияJSON.ПрочитатьJSONИзФайла(ПутьКФайлу);
	СсылкаЗаказа = ИнтеграцияJSON.ЗагрузитьЗаказКлиентаИзJSON(ДанныеJSON);
	
	Сообщить(СтрШаблон("Создан заказ клиента: %1", СсылкаЗаказа), СтатусСообщения.Обычное);

КонецПроцедуры

#КонецЕсли
